export function computeSimple(str: string): number {
  /*
   * Utiliser split et trim pour produire les tableaux
   *   - des opérateurs
   *   - des opérandes
   * Faites un reduce ensuite sur le tableau des opérandes
   * pour produire le résultat
   */
  return 0;
}
